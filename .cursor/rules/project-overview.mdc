# Wedding Hall — monorepo overview

Wedding-budget **MVP** (Phase 1): email/password auth, onboarding wizard, venue estimate dashboard.

## Packages
- **Production Supabase (cloud)** — project ref `siutibzdbnngrvwlnfok`, URL `https://siutibzdbnngrvwlnfok.supabase.co`. Also set as `SUPABASE_PROJECT_REF` / `SUPABASE_URL` in [`.env.example`](../../.env.example). Use with `npm run env:cloud -- --ref siutibzdbnngrvwlnfok`. Postgres **database password** is separate (CLI `supabase link`, `DATABASE_URL`); never commit it.
- **`client/`** — Vite + React 19 + TypeScript + global CSS (`wh-*` classes). SPA with React Router. Auth (`supabase.auth.*`) in the browser; **all data calls go through the server** via `client/src/shared/lib/api.ts`.
- **`server/`** — Next.js 15 (App Router). Data gateway between the SPA and Supabase. Endpoints: `GET /api/health`, `GET /api/openapi.json`, `POST /api/profiles`, `GET /api/budget`, `PUT /api/budget`. Uses Supabase **anon** key + the user's forwarded JWT — RLS still applies.
- **`packages/shared/`** — `@wedding-hall/shared`. Cross-package types (`Profile`, `WeddingBudget`, payloads) and the venue tier prices. Single source of truth for both client and server.
- **`supabase/`** — Postgres schema + RLS + seed (`schema.sql`, `seed.sql`).

## Docs (read first)
- @PLAN.md — scope
- @RULES.md — binding rules
- @README.md — setup, env, deploy, Supabase URLs
- @AGENTS.md — agent orientation + skill link

## Root scripts
From repo root:
- `npm run dev:client` · `npm run dev:server` · `npm run lint` · `npm run build`
- `npm run env:local` · `npm run env:cloud -- --ref <project-ref>` · `npm run env:push-vercel -- --client-project ... --server-project ...`

## Deploy model
- Production **client**: branch **`master-client`** (or Vercel project root `client`).
- Production **server**: branch **`master-server`** (or Vercel project root `server`).
- Integration: **`main`** / **`master`** + `ci.yml` (lint + build, no deploy).
