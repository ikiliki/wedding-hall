# Wedding Hall — env reference (copy the relevant block to each package).
# See README.md and .cursor/skills/wedding-hall-env-bootstrap/SKILL.md for
# the automated way to populate these via `npm run env:local|cloud|push-vercel`.

# ---- Cloud Supabase (production; ref is not secret — appears in browser) ----
# SUPABASE_PROJECT_REF=siutibzdbnngrvwlnfok
# SUPABASE_URL=https://siutibzdbnngrvwlnfok.supabase.co

# ---- Client (Vite) — client/.env.local ----
# VITE_SUPABASE_URL=https://siutibzdbnngrvwlnfok.supabase.co
# VITE_SUPABASE_ANON_KEY=
# VITE_SERVER_URL=http://localhost:3001

# ---- Server (Next) — server/.env.local ----
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# CLIENT_ORIGIN=
