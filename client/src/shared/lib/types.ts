// Re-exports from the monorepo's single source of truth so existing
// `@/shared/lib/types` imports keep working. New code can import from
// `@wedding-hall/shared` directly if it prefers.
export * from "@wedding-hall/shared/types";
