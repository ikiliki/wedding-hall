// Re-exports from `@wedding-hall/shared` so the client UI keeps a stable
// import path. Tier numbers live in `packages/shared/src/venue-pricing.ts`
// (per RULES.md — single source for both client and server).
export * from "@wedding-hall/shared/venue-pricing";
