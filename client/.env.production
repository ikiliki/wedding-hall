# Vite picks this up automatically when running `vite build`
# (which Vercel runs at deploy time). Setting `VITE_SERVER_URL` on the
# client Vercel project overrides this — leave it unset there to use
# this default. Override locally with `client/.env.production.local`.
VITE_SERVER_URL=https://wedding-hall-server.vercel.app
