# Vite picks this up automatically when running `vite` / `vite dev`
# (and inside docker compose, which also runs `vite dev`).
# Override locally with `client/.env.local` if you want to point the
# dev client at a deployed API instead of the docker / native server.
VITE_SERVER_URL=http://localhost:3001
